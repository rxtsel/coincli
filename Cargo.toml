[package]
name = "coincli"
version = "0.1.0"
edition = "2024"

[dependencies]
num-format = "0.4.4"
reqwest = { 
  version = "0.13.3",
  features = [
  "blocking",
  "json",
  ]
}
serde = { version = "1", features = ["derive"] }
